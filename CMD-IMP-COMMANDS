To hide files (not folders) use the command is "attrib +h +s +r enter". To unhide them, use the command "attrib -h -s -r enter" 

|----> COMMMAND To find out wifi passkey:
    ==>  " netsh wlan show profile name="network name" key=clear "   <==
